# https://travis-ci.org/HearthSim/twitch-hdt-ebs
language: python
python: "3.6"

env:
  - TOXENV=py36-django22
  - TOXENV=flake8
  - TOXENV=mypy

cache:
  directories:
    - $HOME/.cache/pip

install:
  - pip install tox

script:
  - tox
